services:
  - type: web
    name: idea-generation-backend
    env: python
    runtime: python-3.11.7
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: idea-generation-db
          property: connectionString
      - key: API_SECRET_KEY
        generateValue: true
    
databases:
  - name: idea-generation-db
    databaseName: ideas
    user: ideas_user
    postgresMajorVersion: 15